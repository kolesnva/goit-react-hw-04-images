import { ErrorContainer } from './SearchErrorStyled';

export function SearchError() {
  return (
    <ErrorContainer>
      There is nothing was found. Try to find something else
    </ErrorContainer>
  );
}
